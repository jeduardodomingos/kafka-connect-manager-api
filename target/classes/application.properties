application.version=1.0-SNAPSHOT
application.name=kafka-connect-manager-api
server.servlet.context-path=/api
server.port=${APPLICATION_PORT}

#spring.datasource.url=${DB_CONNECTION_URL}
#spring.datasource.username=${DB_USER}
#spring.datasource.password=${DB_PASSWORD}
#spring.jpa.hibernate.ddl-auto=none
#spring.jpa.show-sql=true
#spring.jpa.properties.hibernate.format_sql=false


#flyway.url=${FLYWAY_DB_CONNECTION_URL}
#flyway.schemas=${FLYWAY_DB_SCHEMAS}
#flyway.user=${FLYWAY_DB_USER}
#flyway.password=${FLYWAY_DB_PASSWORD}

kafka.connect.api.host=http://localhost:8083